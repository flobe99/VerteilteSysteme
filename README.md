# VerteilteSysteme
Repository für das Testat Verteilte Systeme

# Requirements
Installieren der Requirements mit PIP:
`pip install -r requirements.txt`

# Ausführen der Serveranwendung:
`./server/server.py`

# Ausführen der Clientanwednung:
`./client/client.py`
